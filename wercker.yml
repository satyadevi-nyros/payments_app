box: python:2.7

#build:
#    steps:
#        - virtualenv

#build:
#    steps:
#        - pip-install

build:
    steps:
        - install-packages:
            packages: openssh-client salt-minion

        - script:
                name: 
                code: 

                  #pip install -r requirements.txt
                  #pip install psycopg2
                  python manage.py test
