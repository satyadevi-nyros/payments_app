box: python-slim

build:
    steps:
        - virtualenv

build:
    steps:
        - pip-install

build:
    steps:
        - install-packages:
            packages: openssh-client

        - script:
                name: python unit test
                code: |
                  pip install -r require.txt
                  python manage.py test                   
