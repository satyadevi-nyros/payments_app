box: python:2.7

build:
    steps:
        - install-packages:
            packages: 

        - script:
                name: python unit test
                code: |
                  

                  #pip install -r requirements.txt
                  #pip install psycopg2
                  python manage.py test
